# Hostinger env for alkinda.com — set in hPanel Node.js → Environment variables
# Do not commit real secrets.

# —— Portal (alkinda.com) ——
NEXT_PUBLIC_PORTAL_NAME=Alkinda
NEXT_PUBLIC_PORTAL_DOMAIN=alkinda.com
NEXT_PUBLIC_LABEL=alkinda

# —— Hostinger MySQL for alkinda.com (created in hPanel) ——
# Node.js must use the Remote MySQL hostname, not localhost.
# Password is set only in hPanel environment variables — never commit it.
DB_HOST=srv1764.hstgr.io
DB_PORT=3306
DB_USER=u337841818_lineage_user
DB_PASSWORD=
DB_NAME=u337841818_lineage
